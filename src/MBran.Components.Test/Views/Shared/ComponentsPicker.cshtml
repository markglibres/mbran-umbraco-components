@model ComponentsPicker
@using MBran.Components.Picker
@{ 
    ComponentPickerValue components = Model.ComponentLinks;
}
@foreach (var component in components.Sources)
{
    @Html.Component(components.ComponentType, component)
}